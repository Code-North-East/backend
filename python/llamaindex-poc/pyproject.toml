[project]
name = "llamaindex-poc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "loguru>=0.7.3",
    "python-dotenv>=1.2.2",
]

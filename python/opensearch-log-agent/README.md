# OpenSearch Log Agent

## Configuration

### GitNexus Setup
Use command `npm install -g gitnexus` for installing GitNexus globally.
For setting up the local knowledge graph in your repository, you can use the command `npx gitnexus analyze`.
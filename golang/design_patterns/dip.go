package designpatterns

import "fmt"

// dependency inversion principle -
// dependency principle states that high level modules and low level modules should
// not be tightly coupled with each other, in simple words high level classes should not
// depend on the low level classes directly

func DependencyInversionExample() {
	// todo: add the dependency inversion principle logic
	fmt.Println("Will be adding the example for dependency inversion example soon!")
}